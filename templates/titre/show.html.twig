{% extends 'base.html.twig' %}

{% block title %}Articles{% endblock %}

{% block body %}
    <h1>Articles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ article.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_titre_index') }}">back to list</a>

    <a href="{{ path('app_titre_edit', {'id': article.id}) }}">edit</a>

    {{ include('titre/_delete_form.html.twig') }}
{% endblock %}
